<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <title>Applied Jobs</title>
    <style>
        /* General Reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Poppins', sans-serif;
    background: #333; /* Dark gray theme */
    color: #ddd; /* Light gray text for better contrast */
    min-height: 100vh; /* Ensure full screen height is covered */
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
    padding-top: 50px;
}

/* Navbar */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: #222; /* Darker gray for navbar */
    color: white;
    padding: 15px 30px;
    position: fixed;
    top: 0;
    width: 100%;
    z-index: 10;
}

.navbar-logo a {
    text-decoration: none;
    color: #ff9800; /* Orange color for highlight */
    font-size: 1.5rem;
    font-weight: bold;
}

.navbar-links {
    display: flex;
    gap: 20px;
    list-style: none;
}

.navbar-links li a {
    text-decoration: none;
    color: white;
    font-weight: 600;
    transition: color 0.3s;
}

.navbar-links li a:hover {
    color: #ffcc00;
}

.navbar-toggle {
    display: none;
    cursor: pointer;
}

.navbar-toggle i {
    color: white;
    font-size: 1.5rem;
}

/* Container */
.container {
    margin-top: 100px; /* Spacing below navbar */
    background-color: #222; /* Matches navbar background */
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
    padding: 30px;
    width: 90%; /* Responsive width */
    max-width: 900px; /* Prevents it from getting too wide */
}

.container h2 {
    text-align: center;
    margin-bottom: 20px;
    font-size: 28px;
    color: #ff9800; /* Orange heading */
}

/* Job Cards */
.applied-jobs-list {
    display: flex;
    flex-wrap: wrap;
    gap: 20px;
    justify-content: center;
}

.applied-job-card {
    background-color: #444; /* Slightly lighter gray for contrast */
    border-radius: 8px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    padding: 20px;
    width: 100%; /* Full width in container */
    max-width: 600px; /* Reasonable card size */
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.applied-job-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 15px rgba(0, 0, 0, 0.2);
}

.job-title {
    font-size: 20px;
    font-weight: bold;
    color: #ff9800; /* Orange text */
    margin-bottom: 10px;
}

.job-company {
    font-size: 16px;
    color: #aaa;
    margin-bottom: 15px;
}

.job-description {
    font-size: 14px;
    color: #ccc;
}

.job-salary {
    font-size: 16px;
    color: #ddd;
    margin-top: 10px;
}

/* No Jobs Message */
.no-applied-jobs {
    font-size: 16px;
    color: #888;
    text-align: center;
    margin-top: 50px;
}

/* Responsive Design */
@media (max-width: 768px) {
    .navbar-links {
        display: none;
        flex-direction: column;
        background: rgba(0, 0, 0, 0.9);
        position: absolute;
        top: 60px;
        right: 0;
        width: 100%;
        padding: 10px 0;
    }

    .navbar-links.active {
        display: flex;
    }

    .navbar-toggle {
        display: block;
    }

    .applied-job-card {
        width: 100%; /* Full width on smaller screens */
    }
}


    </style>
</head>

<body>
    <header>
        <nav class="navbar">
            <div class="navbar-logo">
                <a href="/">My Dashboard</a>
            </div> 
            <ul class="navbar-links">
                <li><a href="/student/dashboard">Dashboard</a></li>
                <li><a href="/profile">My Profile</a></li>
                <li><a href="/logout">Logout</a></li>
            </ul>
            <div class="navbar-toggle" id="navbarToggle">
                <i class="fas fa-bars"></i>
            </div>
        </nav>
    </header>

    <div class="container">
        <h2>Your Applied Jobs</h2>

        <% if (appliedJobs.length > 0) { %>
            <div class="applied-jobs-list">
                <% appliedJobs.forEach(job => { %>
                    <div class="applied-job-card">
                        <h3 class="job-title"><%= job.title %></h3>
                        <p class="job-company"><%= job.company %></p>
                        <p class="job-description"><%= job.description %></p>
                        <p class="job-salary"><%= job.salary %></p>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <p class="no-applied-jobs">No applied jobs yet.</p>
        <% } %>
    </div>

    <script>
        const navbarToggle = document.getElementById('navbarToggle');
        const navbarLinks = document.querySelector('.navbar-links');

        navbarToggle.addEventListener('click', () => {
            navbarLinks.classList.toggle('active');
        });
    </script>
</body>

</html>
