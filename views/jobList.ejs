<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Job Listings</title>
    <style>
        /* Add your styles here */
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            padding: 20px;
        }

        .job-container {
            background: white;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
        }

        h2 {
            text-align: center;
        }

        .job-title {
            font-size: 1.5rem;
            color: #007bff;
        }

        .job-description {
            margin: 10px 0;
        }

        .applicants-count {
            font-weight: bold;
        }
    </style>
</head>

<body>
    <h2>Job Listings</h2>
    <% jobs.forEach(job => { %>
        <div class="job-container">
            <div class="job-title"><%= job.name %></div>
            <div class="job-description"><%= job.description %></div>
            <div class="applicants-count">Applicants: <%= job.applicants.length %></div>
            <a href="<%= job.applyLink %>" target="_blank">Apply Now</a>
        </div>
    <% }) %>
</body>

</html>